import type { RuntimeOperationalSnapshot, RuntimeOperationalState, RuntimeOperationalStateValidationReport } from './runtime-operational-state-types.js';
export function validateRuntimeOperationalState(snapshot: unknown): RuntimeOperationalStateValidationReport { const errors: string[] = []; const s = snapshot as RuntimeOperationalSnapshot; if (!s || typeof s !== 'object') errors.push('snapshot_not_object'); if (!s?.continuity?.runtimeSessionId) errors.push('missing_runtime_session_id'); if (!s?.continuity?.orchestrationSessionId) errors.push('missing_orchestration_session_id'); if ((s?.continuity?.operationalSequenceNumber ?? -1) < (s?.lifecycle?.length ?? 0) - 1) errors.push('sequence_out_of_order'); if (s?.metadata?.snapshotVersion !== 1) errors.push('snapshot_version_incompatible'); if ((s?.counters?.replayDenials ?? 0) < (s?.replay?.deniedNonces?.length ?? 0)) errors.push('replay_counter_mismatch'); return { valid: errors.length === 0, errors }; }
export function hydrateRuntimeOperationalState(snapshot: RuntimeOperationalSnapshot): RuntimeOperationalState { const report = validateRuntimeOperationalState(snapshot); if (!report.valid) throw new Error(`invalid_runtime_operational_snapshot:${report.errors.join(',')}`); return JSON.parse(JSON.stringify(snapshot)) as RuntimeOperationalState; }
export function dehydrateRuntimeOperationalState(state: RuntimeOperationalState): RuntimeOperationalSnapshot { return JSON.parse(JSON.stringify(state)) as RuntimeOperationalSnapshot; }
