{
  "name": "@aoc-enterprise/access-grant",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise-owned, provider-neutral, immutable record of an issued authorization: what access was granted, to whom, for which resource, referencing the EnterpriseAccessDecision and EnterpriseAccessObligations that authorized it. Contains no provider credential, JWT, OAuth token, signed URL, runtime session, or adapter.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test 'dist-test/__tests__/**/*.test.js'"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@aoc-enterprise/resource-envelope": "file:../resource-envelope"
  }
}
