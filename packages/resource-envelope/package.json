{
  "name": "@aoc-enterprise/resource-envelope",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise-owned, provider-neutral envelope describing a governed external resource. Composes AOC Protocol's ResourceRef; contains no credential, provider SDK type, or runtime logic.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test 'dist-test/__tests__/**/*.test.js'"
  },
  "files": [
    "dist"
  ]
}
