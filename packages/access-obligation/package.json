{
  "name": "@aoc-enterprise/access-obligation",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise-owned, provider-neutral, declarative record of a mandatory or optional condition attached to an evaluated EnterpriseAccessDecision. Contains no policy engine, no execution, no grant, no provider, and no runtime object.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test 'dist-test/__tests__/**/*.test.js'"
  },
  "files": [
    "dist"
  ]
}
