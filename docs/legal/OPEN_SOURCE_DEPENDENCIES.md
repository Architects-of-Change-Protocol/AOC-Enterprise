# Open Source Dependency Inventory

> STATUS: DRAFT — PENDING PROFESSIONAL LEGAL REVIEW.
> Generated by inspecting `package.json` manifests and `package-lock.json`
> (root, lockfileVersion 3) on 2026-07-14. This is a documentation pass
> only — no dependency was added, removed, or upgraded as part of this
> sprint. This corroborates, and does not replace, the existing
> engineering-level audit in `docs/release/DEPENDENCY_AUDIT_V1.md`.

## Method

License values below come from the `license` field recorded for each
package in the root `package-lock.json` (npm records this from each
package's own published `package.json` at resolution time). Where no
`license` field is recorded in the lockfile, this document says so
explicitly rather than guessing. Workspace-internal packages
(`@aoc-enterprise/*`, `packages/*`, `apps/*`) are excluded from this
table — they are Soberanía Enterprise's own proprietary code, not third-party
dependencies (see `docs/legal/IP_OVERVIEW.md`). `@aoc/protocol` is
likewise excluded — it is a peer dependency on the separate Soberanía Protocol
project, not a conventional open-source dependency (see
`docs/legal/PROTOCOL_ENTERPRISE_BOUNDARY.md`).

The root lockfile is the single source of truth for the whole npm
workspaces monorepo (`workspaces: ["packages/*", "apps/*"]`) and
resolves 96 unique third-party/transitive package entries in total.

## Direct dependencies (declared in a `package.json`)

| Package | Version | License | Uso | Distribución | Riesgo | Evidencia |
|---|---|---|---|---|---|---|
| `better-sqlite3` | 12.11.1 | MIT | Root production dependency — SQLite persistence for governance/evidence/passport/assurance stores | Bundled into built artifacts; lazily `await import`-ed only when the `sqlite` provider is selected | Low — permissive, native module with prebuilt binaries | `package.json` (root `dependencies`), `package-lock.json` |
| `react` | 18.3.1 | MIT | Root optional peer dependency; production dependency of `apps/agent-passport-web` | Consumer-supplied (peer) / bundled in the demo app build | Low — permissive | `package.json` (root `peerDependencies`, optional) |
| `react-dom` | 18.3.1 | MIT | Same as `react` | Same as `react` | Low | Same |
| `next` | 14.2.35 | MIT | Production dependency of `apps/agent-passport-web` (private demo app only) | Not part of the shipped Enterprise runtime deliverable | Low (license) — see Note below on security advisories (a separate concern from licensing) | `apps/agent-passport-web/package.json` |
| `stripe` | 22.3.0 | MIT (SDK) | Production dependency of `apps/agent-passport-web` for billing/checkout | Not part of the shipped Enterprise runtime deliverable | Low (SDK license) — **Commercial**: using the Stripe *service* (as opposed to the MIT-licensed SDK) requires a separate Stripe merchant account and Stripe's own Terms of Service, which is a commercial/contractual matter, not an open-source license matter | `apps/agent-passport-web/package.json` |
| `typescript` | 5.9.3 (root lockfile) / `^5.5.4` declared in `apps/agent-passport-web` | Apache-2.0 | Dev-only, build tooling | Not distributed | Low | `package-lock.json` |
| `@types/better-sqlite3` | 7.6.13 | MIT | Dev-only, type definitions | Not distributed | Low | `package-lock.json` |
| `@types/react`, `@types/react-dom` | 18.3.31 / 18.3.7 | MIT | Dev-only, type definitions | Not distributed | Low | `package-lock.json` |
| `@types/node` | 20.19.43 | MIT | Production dependency of `apps/agent-passport-web` (type definitions; effectively dev-only in practice) | Not distributed | Low | `apps/agent-passport-web/package.json` |

## Transitive dependencies of note

The full transitive tree (96 lockfile entries) is dominated by
`better-sqlite3`'s native-module build toolchain (`bindings`,
`prebuild-install`, `node-abi`, `tar-fs`, `tar-stream`, `simple-get`,
`decompress-response`, etc. — all MIT/ISC/Apache-2.0/BSD) and `next`'s
own dependency tree (`@next/*` platform binaries, `@swc/*`, `postcss`,
`styled-jsx`, `scheduler`, etc. — all MIT/Apache-2.0/BSD/CC-BY-4.0/0BSD).
Consistent with `docs/release/DEPENDENCY_AUDIT_V1.md`: **no copyleft
(GPL/AGPL/LGPL) or SSPL-licensed package was found anywhere in the
tree.**

Two lockfile entries have no `license` field recorded and require
manual verification before external distribution:

| Package | Version | License | Uso | Distribución | Riesgo | Evidencia |
|---|---|---|---|---|---|---|
| `busboy` | 1.6.0 | License status requires manual verification. | Transitive dependency of `next` (multipart form parsing) | Bundled with `next` in the demo app | Unknown until verified | `package-lock.json` (no `license` field recorded) |
| `streamsearch` | 1.1.0 | License status requires manual verification. | Transitive dependency of `busboy` | Bundled with `next` in the demo app | Unknown until verified | `package-lock.json` (no `license` field recorded) |

Non-code / dual-licensed entries worth noting explicitly:

| Package | Version | License | Note |
|---|---|---|---|
| `caniuse-lite` | 1.0.30001799 | CC-BY-4.0 | Browser-support **data**, not executable code; consumed by `next`'s build tooling |
| `expand-template` | 2.0.3 | (MIT OR WTFPL) | Dual-licensed; MIT terms apply for this project's purposes |
| `rc` | 1.2.8 | (BSD-2-Clause OR MIT OR Apache-2.0) | Multi-licensed; any of the three permissive options apply |
| `tslib` | 2.8.1 | 0BSD | Public-domain-equivalent permissive license |

## Classification summary

- **Permissive:** MIT, Apache-2.0, ISC, BSD-2-Clause, BSD-3-Clause,
  0BSD, CC-BY-4.0 (data) — all direct and nearly all transitive
  dependencies fall here.
- **Weak copyleft:** none found.
- **Strong copyleft:** none found.
- **Unknown:** `busboy`, `streamsearch` (see above — license status
  requires manual verification).
- **Commercial:** none in the sense of a paid software license; `stripe`
  is a commercial *service* accessed through an MIT-licensed SDK (see
  above).
- **Proprietary:** none — all workspace-internal packages are
  Onchainfest LLC's own proprietary code and are intentionally excluded
  from this third-party inventory (see `docs/legal/IP_OVERVIEW.md`).
- **Dual licensed:** `expand-template`, `rc` (see above).

## Vendored code, non-code assets, fonts, images, commercial SDKs

- **Vendored/copied third-party code:** none found (`vendor/`,
  `third_party/`, `third-party/` directories do not exist in this
  repository).
- **Non-code assets (fonts, images):** none identified as part of this
  review; if product/marketing assets are added later (e.g. under
  `apps/agent-passport-web/public/`), they should be added to this
  inventory with their own licensing terms.
- **Commercial SDKs:** `stripe` (see above) is the only commercial-service
  SDK identified.

## Known repository hygiene gaps (not license risks, but affect
reproducibility of this inventory)

- `apps/agent-passport-web` has its own standalone `package-lock.json`
  in addition to being resolved through the root workspace lockfile —
  this could drift from the root-resolved versions over time and should
  be reconciled or removed by engineering.
- Several packages' test scripts reference `tsx` (e.g. via
  `node --import tsx --test`), but `tsx` is not declared as a dependency
  anywhere in this repository — a build-reproducibility gap, not itself
  a licensing determination, worth engineering follow-up.

## Out of scope for this pass

No dependency was upgraded, downgraded, removed, or added as part of
this legal/IP hardening sprint. This document is a point-in-time
inventory; it should be regenerated whenever dependencies change, ideally
via automated SBOM tooling (no SPDX/CycloneDX SBOM currently exists in
this repository — see `docs/legal/IP_DUE_DILIGENCE_CHECKLIST.md`).
