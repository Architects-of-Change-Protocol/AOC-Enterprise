{
  "name": "external-consumer-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --pretty false --noEmit",
    "build": "tsc --pretty false"
  }
}
